% -------------------------------------------------
% Layout
% -------------------------------------------------

\usepackage[
	margin=2cm,
	includehead,
	includefoot,
	footskip=15mm,
]{geometry}

% Paragraphs
\setlength{\parindent}{0pt}
\setlength{\parskip}{0.5\baselineskip}

% Number headings down to subparagraph
\setcounter{secnumdepth}{5}

% Include headings down to subparagraph in TOC
\setcounter{tocdepth}{5}
